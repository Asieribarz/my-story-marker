# dif-local-vm.md — Diferencias de esta máquina

Lo que en esta máquina funciona distinto de lo que dan por hecho `specs/contexto-tras-bloque1.md` y `docs/`, y el camino alternativo que se ha tomado en cada caso. Se va ampliando a medida que aparece algo nuevo.

| # | Fecha | Qué pasa | Camino alternativo | Afecta a |
|---|---|---|---|---|
| L-1 | 2026-09-23 | `uv` no estaba instalado. | `python -m pip install --user uv`. El ejecutable queda en `%APPDATA%\Python\Python312\Scripts`, que **no está en el PATH**: hay que anteponerlo (`export PATH="$APPDATA/Python/Python312/Scripts:$PATH"` en bash). | Todas las comprobaciones |
| L-2 | 2026-09-23 | Una directiva de **Control de aplicaciones de Windows** bloquea la DLL nativa de `tiktoken` (`_tiktoken`): `ImportError: DLL load failed ... Una directiva de Control de aplicaciones bloqueó este archivo`. Es una política de la máquina, no un fallo del código, y no se intenta saltar. | El estimador (RF-52a) usa `tiktoken` si carga y, si no, un BPE en Python puro sobre el **mismo** fichero `o200k_base` versionado en `backend/capitulo/bpe/`, con el mismo factor ×1,35. Las pruebas que necesitan la DLL se saltan aquí, con el motivo; la de paridad entre los dos caminos corre donde `tiktoken` carga. | Recuperador (paso 6), `test_bpe.py` |
| L-2b | 2026-09-23 | El respaldo de L-2 está en `backend/capitulo/estimador.py`. Reproduce los ids de o200k (`"Hello, world!"` → `[13225, 11, 2375, 0]`) y estima ~600.000 caracteres en 0,4 s. Su pre-tokenización con `re` (clases `\p{..}` generadas desde `unicodedata`) coincide con la de `regex` en 21.346 piezas. | Se usa solo `re`: `regex` es una dependencia transitiva de `tiktoken` sin declarar. La paridad exacta con `tiktoken` solo se puede medir en una máquina donde cargue la DLL. | Recuperador |
| L-3 | 2026-09-23 | No hay en el PATH ni `node`, ni `java`, ni `elan`/`lake`, ni el CLI `claude`. | Se construye sin ellos: el worker se prueba con un `claude` falso (TC-10), y las pruebas de Lean, TLC y Chromium se saltan si falta la herramienta. Cada instalación se pregunta cuando haga falta. | Bloque 3, frontend, `formal/`, paso S |
| L-5 | 2026-09-23 | Se temía que la política bloquease también el `node.exe` y el Chromium de Playwright. **No los bloquea:** `playwright` 1.63 imprime PDF con su Chromium (`--only-shell`) y con `channel="msedge"`. | Ninguno: aquí funciona el camino principal de TC-6. | Paso 9 |
| L-6 | 2026-09-24 | Git tiene `core.autocrlf=true`: el código se escribe con LF y Git avisa de que lo pasará a CRLF en la copia de trabajo. Los documentos que ya estaban en CRLF siguen así. | Ninguno necesario: el BPE está marcado como binario en `.gitattributes` (su SHA-256 no cambia) y en el repositorio se guarda LF. | Commits |
| L-7 | 2026-09-24 | La sesión de Claude Code se cerró durante la noche y dos agentes se quedaron parados por el vigilante de 10 minutos sin progreso. | Se reanudaron desde su transcripción con SendMessage, sin perder trabajo. | Tiempos |
| L-4 | 2026-09-23 | El repositorio está en `%USERPROFILE%\my-story-marker`, no en `C:\Users\student\Documents\GitHub\my-story-marker`. El trabajo del bloque 2 a medias ya llega commiteado (`d99cb59`). | Las rutas absolutas de `specs/contexto-tras-bloque1.md` y `specs/workflow-bloque-2.js` se leen con la ruta nueva. | Traspaso |
